// Lex doesn't provide any typings, so we need to add this module declaration.
// Anything imported from Lex will have a type of `any`.
// We can also specify our own typings for the library here.

declare module '@uncharted.software/lex/dist/lex';
