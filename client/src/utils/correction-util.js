// Emulates an enum
export const CORRECTION_TYPES = Object.freeze({
  POLARITY: 1,
  ONTOLOGY_ALL: 2,
  ONTOLOGY_SUBJ: 3,
  ONTOLOGY_OBJ: 4,
  POLARITY_UNKNOWN: 5
});

// Emulates an enum
export const CURATION_STATES = Object.freeze({
  NON_EVALUATED: 0,
  EDITED: 1,
  VETTED: 2
});
