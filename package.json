{
  "private": true,
  "name": "causemos",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "start-client": "yarn workspace client run serve",
    "start-server": "yarn workspace server run start --morgan-format dev",
    "start-server-debug": "yarn workspace server run start --morgan-format dev --log-level debug",
    "start-dev": "concurrently \"yarn workspace server run dev\" \"yarn workspace client run serve\""
  },
  "devDependencies": {
    "concurrently": "^6.2.1"
  },
  "dependencies": {}
}
